CIRCUS COMMANDS:

    - Get list of watchers or processes in a watcher:

        Client circus_client = Client(context, host, port, timeout);
        Error error;
        circus_client.list("<name>", error);
        circus_client.list(error);


        Command-line equivalent:
            $ circusctl list [<name>]
